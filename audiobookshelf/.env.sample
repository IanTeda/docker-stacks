# AUDIOBOOKSHELF - environment sample
# Copy to `.env` and edit values as needed

# --- User & timezone ---
# Run `id $user` to check your PUID/PGID
PUID=1000
PGID=1000
TZ=Etc/UTC

# --- Audiobookshelf image / ports ---
# https://github.com/advplyr/audiobookshelf/releases
AUDIOBOOKSHELF_VERSION=v2.32.1
AUDIOBOOKSHELF_PORT=13378

# --- Paths (do NOT use ~ in compose defaults) ---
AUDIOBOOKSHELF_PATH=/opt/audiobookshelf
AUDIOBOOKS_PATH=/media/audiobooks
PODCASTS_PATH=/media/podcasts

# --- Optional Traefik / reverse-proxy values ---
# Set these if you want Traefik to route traffic to Audiobookshelf
TRAEFIK_ENABLED=false
TRAEFIK_AUDIOBOOKSHELF_HOSTNAME=audiobookshelf.example.com
TRAEFIK_AUDIOBOOKSHELF_URL_PATH=/subpath
