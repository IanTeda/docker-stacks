# Traefik Environment Variables Sample
#
# Copy this file to .env and update the values below.
# This file contains all environment variables needed for Traefik configuration.
#
# Version: 1.0
# Last Updated: 2026-02-16

# Set if you want to freeze a version
# https://hub.docker.com/_/traefik/tags
TRAEFIK_VERSION=latest

# Path to store Traefik data (e.g. acme.json for Let's Encrypt certificates)
TRAEFIK_PATH=./.volume

# Generate with: # Generate with: `$ python3 -c "import bcrypt; print('<username>:' + bcrypt.hashpw(b'<password>', bcrypt.gensalt()).decode())" | sed -e 's/\$/\$\$/g'`
# Replace with your own credentials hash. Use single quotes to avoid shell interpolation issues. Example format: 'username:hashed_password'
TRAEFIK_DASHBOARD_CREDENTIALS='<past_echo_result_hash>' 

# Cloudflare Dashboard / Profile / API Tokens
CF_DNS_API_TOKEN=CopyFromCloudflareAPIToken
CF_API_EMAIL=your_email@example.com

# Name of the docker host
HOSTNAME=<hostname_of_docker_host>

# Domain name the host is part of
DOMAIN=<domain_name_the_host_is_part_of>