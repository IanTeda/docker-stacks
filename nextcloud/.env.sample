# -----------------------------------------------------------------------------
# Nextcloud Docker Compose Configuration
# Copy and rename this file to .env and configure the variables below.

# --- User & Group ID ---
# Run `id $user` to check your PUID/PGID
PUID=1000
PGID=1000

# --- Timezone ---
TZ=Etc/UTC

# --- Nextcloud Configuration ---
# Version tag for Nextcloud (e.g., latest, 32.0.5-ls414)
# https://github.com/linuxserver/docker-nextcloud/releases
NEXTCLOUD_VERSION=latest

# Storage path for Nextcloud data and config
# If not set, defaults to ./.data inside the project directory
NEXTCLOUD_PATH=.data

# --- PostgreSQL Database Configuration ---
# Database user and name (defaults to 'nextcloud' if not set)
# https://hub.docker.com/_/postgres
POSTGRES_VERSION=latest
POSTGRES_USER=nextcloud
POSTGRES_DB=nextcloud
POSTGRES_PASSWORD=replace_with_strong_password

# --- Redis Configuration ---
# https://hub.docker.com/_/redis
REDIS_VERSION=latest
REDIS_PASSWORD=replace_with_strong_redis_password

# --- Maintenance ---
# Enable Watchtower to automatically update containers (true/false)
WATCHTOWER_UPDATE_ENABLE=false
